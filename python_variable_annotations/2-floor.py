#!/usr/bin/env python3
"""This modue returns the floor of the float. """
import math


def floor(n: float) -> int:
    """Return the floor of the float."""
    floor_n = math.floor(n)
    return floor_n
